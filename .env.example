ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=

# Optional - defaults to a local Postgres instance (see db.py). Only set
# this if you're pointing at a different Postgres server/database.
# SIMSTARS_DATABASE_URL=postgresql+psycopg:///simstars
